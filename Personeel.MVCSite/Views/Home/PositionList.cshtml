@model IEnumerable<Personeel.MVCSite.Models.PositionViewModels.AddPosition>

@{
    ViewBag.Title = "PositionList";
}

<h2>PositionList</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PosName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PosDesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PosName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PosDesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
